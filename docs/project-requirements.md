# 轻量级用户态动态探针设计与实现

本文档是对 `docs/` 下官方技术方案和章程 PDF 中项目相关条款的归纳整理，用于仓库内对照实现和验证；原始要求以官方 PDF 为准。

术语约定：题面中的 `ARM64` 在本仓库实现和文档中统一写作 `aarch64`，二者均指 64 位 Arm Linux 用户态目标架构。

## 1. 赛题背景
在现代系统可观测性、安全监控和性能分析中，动态追踪（Dynamic Tracing）技术扮演着关键角色。Linux 内核原生提供了 uprobe 机制，允许在不修改、不重启目标程序的前提下，在用户态函数入口处插入探针，用于采集参数、返回值或执行自定义逻辑。

Linux 内核提供的 uprobe 机制，原理是修改目标进程指定地址的指令，插入 trap 指令陷入内核态，在内核态里执行探针。一方面，trap 指令陷入内核，对一些性能敏感的函数有不可忽视的影响；另一方面，尽管可以通过 ebpf 编程实现探针，但受限于目标进程符号及结构体信息的缺失，探针的表达能力并不太强。

鉴于 Linux 内核 uprobe 机制存在的上下文开销和有限的表达力，本赛题提出轻量级用户态动态探针的设计，参考 Linux 内核 kprobe 的设计和实现，借助 ptrace 系统调用可以修改指定进程内存的能力，在进程内存里写入探针代码，并修改指定地址的指令，实现不陷入内核态，直接在用户态跳转执行探针代码的轻量级用户态动态探针的能力。

本赛题旨在引导参赛者深入理解进程内存布局，指令编码，上下文保存恢复、信号与异常处理、ptrace 等操作系统知识并实践

---

## 2. 赛题任务要求

### 基础功能 (F1 - F7) —— 必选
- **F1**: 动态库函数探针插入（用户态跳转，不产生 trap 陷入内核）

- **F2**: 函数参数获取（至少前 6 个参数，遵循目标架构 ABI）

- **F3**: 函数返回值捕获（return probe）

- **F4**: 探针清理（恢复原始指令，无内存泄漏）

- **F5**: 探针动态开关（enable/disable 切换）

- **F6**: 多探针共存（同一进程至少 16 个并发探针）

- **F7**: 多线程安全（不导致崩溃或死锁）

### 进阶功能 (A1 - A5) —— 加分项
- **A1**: 多架构支持（x86_64 + ARM64）

- **A2**: 条件探针（参数过滤）

- **A3**: 与 perf/ftrace 事件流合流（输出与 perf-script 兼容的事件格式，使用 CLOCK_MONOTONIC 时间戳，可与 ftrace/kprobe 事件合并分析）

- **A4**: 探针内调用目标进程函数

- **A5**: 探针热更新

---

## 3. 技术指标

### 性能指标
- 单次探针执行开销 < 1000ns（对比 uprobe 约 1500-3000ns）

- 探针插入/清理延迟 < 10ms

- 无探针触发时零开销

### 正确性指标
- 上下文保存/恢复完整性（通用寄存器、标志寄存器、浮点寄存器）

- 多线程并发安全

- ASLR/PIE 兼容

- 信号安全

---

## 4. 测试基准与评分

### 标准化测试方案 (Benchmark)
提供标准化测试方案：

- 目标进程循环调用 getpid 1000000 次

- 分别测量无探针 / 内核 uprobe / 用户态探针的执行时间

- 计算单次开销并生成对比报告

### 评分权重分布
- **基础功能 (F1-F7)**：50%
- **正确性/稳定性**：20%
- **性能指标**：15%
- **文档完整性与 Demo 演示**：15%
- **进阶功能 (A1-A5)**：按实现复杂度额外加分。

---

## 5. 通用参赛交付要求

技术方案 1.1 还包含与代码实现同等重要的通用交付要求：

- 参赛作品源代码需遵循 GPL、Apache、BSD、木兰协议中的至少一种；本仓库根目录已保留 `LICENSE`，当前为 GPLv3。
- 技术文档和答辩材料需按比赛要求采用开放授权方式发布。
- 阶段性和最终提交材料除源代码外，还应包含设计方案文档、进展汇报幻灯片和作品演示视频；现场答辩材料也应与代码和设计文档保持一致。
- 决赛结束后，最终版本作品及相关开发资料需要保持开源、开放并可公开访问。
- 作品需要通过多次 git commit 展示持续迭代过程，提交信息应能说明新增功能、修复或关键改进，避免无说明的批量提交。
- 若使用 AI / 大模型 / AI Agent 等辅助工具，需要在设计实现文档中声明工具名称、模型名称、使用场景、AI 产出和交互记录说明；本仓库使用 [ai-usage-report.md](./ai-usage-report.md) 记录相关内容。
- 如果引用非本队代码、文档或往届作品，需要在代码位置、设计文档和答辩材料中说明来源、用途、授权信息和本项目增量贡献。

---

## 6. 关键时间节点

章程中与功能挑战赛提交相关的关键日期如下：

- 初赛作品提交截止日：2026 年 6 月 30 日，提交内容包括源代码、设计方案文档、进展汇报幻灯片和作品演示视频。
- 阶段性决赛作品提交截止日：2026 年 8 月 12 日，提交内容同样包括源代码、设计方案文档、进展汇报幻灯片和演示视频。
- 决赛作品提交截止日：2026 年 8 月 18 日（暂定），该作品作为最终评审版本。
- 现场答辩日：2026 年 8 月 19 日（暂定）。
- 决赛结束后，最终版本作品及源代码、设计方案文档、演示视频等开发资料需要保持开源、开放并可公开访问。

---

## 7. 参考资料
* [Linux Uprobe Tracer 官方文档](https://docs.kernel.org/trace/uprobetracer.html)
* [Linux Kprobes 实现原理](https://docs.kernel.org/trace/kprobes.html)
* [Libcare: 用户态热补丁库](https://github.com/cloudlinux/libcare)
* [Ptrace(2) Man Page](https://man7.org/linux/man-pages/man2/ptrace.2.html)
* [Frida 动态插桩框架](https://frida.re/)
* [bpftime: 高性能用户态 eBPF 运行时](https://eunomia.dev/zh/blogs/bpftime/)
